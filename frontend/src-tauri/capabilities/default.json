{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions par défaut du client College Aureole (client léger : aucune API native requise au-delà du cœur).",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
